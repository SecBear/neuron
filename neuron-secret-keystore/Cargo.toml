[package]
name = "neuron-secret-keystore"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Stub secret resolver for OS keystore (macOS Keychain, Windows DPAPI, Linux Secret Service)"

[dependencies]
neuron-secret = { path = "../neuron-secret" }
layer0 = { path = "../layer0" }
async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
