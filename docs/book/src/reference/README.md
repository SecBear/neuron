# Reference

Technical reference material for the neuron workspace:

- **[Crate Map](crate-map.md)** -- Every crate in the workspace, organized by layer, with one-line descriptions.
- **[Error Handling](error-handling.md)** -- Error types, the `thiserror` pattern, and how errors propagate across protocol boundaries.

For API documentation generated from source code, run `cargo doc --no-deps --open`.
