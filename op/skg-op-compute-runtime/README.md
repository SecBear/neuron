# skg-op-compute-runtime

Programmable execution runtime for Skelegent agents.

This crate is under active development. The initial scaffold defines the public execution nouns (`ComputeRuntime`, `ComputeBackend`, `ExecutionProfile`, `ComputeSession`, `ExecutionReport`) while the Python-first implementation lands in later tasks.
