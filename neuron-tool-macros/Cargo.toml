[package]
name = "neuron-tool-macros"
version = "0.2.2"
edition.workspace = true
rust-version.workspace = true
description = "Derive macro for LLM tool definitions — generates JSON Schema and dispatch code from a Rust struct"
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/neuron-tool-macros"
keywords = ["ai", "llm", "tools", "proc-macro", "derive"]
categories = ["science", "development-tools::procedural-macro-helpers"]

[lib]
proc-macro = true

[dependencies]
syn.workspace = true
quote.workspace = true
proc-macro2.workspace = true
